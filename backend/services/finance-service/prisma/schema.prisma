generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/finance-client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// TODO: Add finance models here
