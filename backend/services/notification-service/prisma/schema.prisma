generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/notification-client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// TODO: Add notification models here
