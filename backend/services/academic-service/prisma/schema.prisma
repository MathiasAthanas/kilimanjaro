generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/academic-client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// TODO: Add academic models here
