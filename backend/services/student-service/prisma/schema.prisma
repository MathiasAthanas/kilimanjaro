generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/student-client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// TODO: Add student models here
