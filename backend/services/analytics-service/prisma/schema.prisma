generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/analytics-client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// TODO: Add analytics models here
