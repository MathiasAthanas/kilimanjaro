# Mobile

This folder is reserved for mobile application(s).

Suggested structure:

```text
mobile/
  apps/
    parent-app/
    student-app/
  packages/
    shared-ui/
    shared-types/
```

When ready, initialize your mobile stack here (for example React Native/Expo or Flutter).